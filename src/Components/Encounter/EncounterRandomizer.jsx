import { Typography } from "@material-ui/core";

const EncounterRandomizer = () => {
  return (
    <Typography variant="subtitle2" gutterBottom>
      [Random encounter generator will go here]
    </Typography>
  );
};

export default EncounterRandomizer;
